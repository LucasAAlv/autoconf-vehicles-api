# Authenticating requests

This API is authenticated via a Laravel Sanctum SPA session cookie, not a bearer/header/query token — see the "Authentication" section above for the exact login + CSRF flow to use from these docs.
